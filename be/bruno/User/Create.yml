info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/{{USER_ENDPOINT}}"
  body:
    type: json
    data: |-
      {
        "username": "12345678",
        "email": "user@example.com",
        "name": "Nguyen Van A",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
