info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/{{ROLE_ENDPOINT}}"
  body:
    type: json
    data: |-
      {
        "code": "ADMIN",
        "name": "Administrator",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
