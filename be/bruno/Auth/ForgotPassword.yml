info:
  name: ForgotPassword
  type: http
  seq: 7

http:
  method: POST
  url: "{{API_URL}}/{{AUTH_ENDPOINT}}/forgot-password"
  body:
    type: json
    data: |-
      {
        "username": "12314092",
        "email": "user@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
