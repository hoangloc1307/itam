info:
  name: Update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{API_URL}}/{{USER_ROLE_ENDPOINT}}/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "roleCode": "ADMIN",
        "section": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
