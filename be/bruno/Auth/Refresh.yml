info:
  name: Refresh
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/{{AUTH_ENDPOINT}}/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
