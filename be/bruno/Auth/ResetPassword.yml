info:
  name: ResetPassword
  type: http
  seq: 8

http:
  method: POST
  url: "{{API_URL}}/{{AUTH_ENDPOINT}}/reset-password"
  body:
    type: json
    data: |-
      {
        "username": "12314092",
        "code": "123456",
        "newPassword": "newpass123",
        "confirmPassword": "newpass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
