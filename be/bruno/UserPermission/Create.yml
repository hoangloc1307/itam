info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/{{USER_PERMISSION_ENDPOINT}}"
  body:
    type: json
    data: |-
      {
        "username": "admin001",
        "featureCode": "CATEGORY",
        "action": "READ",
        "decision": "ALLOW",
        "section": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
