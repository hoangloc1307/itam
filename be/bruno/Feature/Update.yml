info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: '{{API_URL}}/{{FEATURE_ENDPOINT}}/:code'
  params:
    - name: code
      value: REPORT
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Reports Updated",
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
