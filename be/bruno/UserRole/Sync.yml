info:
  name: Sync
  type: http
  seq: 4

http:
  method: PUT
  url: "{{API_URL}}/{{USER_ROLE_ENDPOINT}}/sync"
  body:
    type: json
    data: |-
      {
        "username": "admin001",
        "roles": [
          {
            "roleCode": "ADMIN",
            "section": null
          },
          {
            "roleCode": "USER",
            "section": "6100"
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
