info:
  name: ResetPassword
  type: http
  seq: 5

http:
  method: POST
  url: "{{API_URL}}/{{USER_ENDPOINT}}/:username/reset-password"
  params:
    - name: username
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
