info:
  name: GetByCode
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/{{ROLE_ENDPOINT}}/:code"
  params:
    - name: code
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
