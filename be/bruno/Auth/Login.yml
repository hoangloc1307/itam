info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/{{AUTH_ENDPOINT}}/login"
  body:
    type: json
    data: |-
      {
        "username": "12314092",
        "password": "123456"
      }
  auth: inherit

runtime:
  variables:
    - name: ""
      value: ""
  scripts:
    - type: after-response
      code: bru.setEnvVar("TOKEN", res('data.token'));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
