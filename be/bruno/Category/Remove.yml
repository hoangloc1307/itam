info:
  name: Remove
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{API_URL}}/{{CATEGORY_ENDPOINT}}/:id"
  params:
    - name: id
      value: LAPTOP
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
