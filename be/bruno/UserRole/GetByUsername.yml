info:
  name: GetByUsername
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/{{USER_ROLE_ENDPOINT}}/:username"
  params:
    - name: username
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
