info:
  name: Remove
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{API_URL}}/{{FEATURE_ENDPOINT}}/:code'
  params:
    - name: code
      value: REPORT
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
