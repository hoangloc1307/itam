info:
  name: GetByCategoryId
  type: http
  seq: 1

http:
  method: GET
  url: '{{API_URL}}/category-attributes/:categoryId'
  auth: inherit

params:
  path:
    - name: categoryId
      value: ''

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
