info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: '{{API_URL}}/{{FEATURE_ENDPOINT}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
