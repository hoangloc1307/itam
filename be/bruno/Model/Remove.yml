info:
  name: Remove
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{API_URL}}/{{MODEL_ENDPOINT}}/:id'
  params:
    - name: id
      value: MBP14-16-512
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
