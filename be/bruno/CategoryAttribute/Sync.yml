info:
  name: Sync
  type: http
  seq: 2

http:
  method: PUT
  url: '{{API_URL}}/category-attributes/:categoryId'
  body:
    type: json
    data: |-
      {
        "attributes": [
          {
            "attributeId": 1,
            "sortOrder": 0,
            "isRequired": false
          }
        ]
      }
  auth: inherit

params:
  path:
    - name: categoryId
      value: ''

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
