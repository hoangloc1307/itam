info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/{{ROLE_PERMISSION_ENDPOINT}}"
  body:
    type: json
    data: |-
      {
        "roleCode": "ADMIN",
        "featureCode": "CATEGORY",
        "action": "READ",
        "section": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
