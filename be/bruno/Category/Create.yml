info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/{{CATEGORY_ENDPOINT}}"
  body:
    type: json
    data: |-
      {
        "id": "CAT001",
        "name": "Laptop",
        "serialKey": "LPT",
        "maintenanceIntervalHours": 8760
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
