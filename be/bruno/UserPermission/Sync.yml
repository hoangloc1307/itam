info:
  name: Sync
  type: http
  seq: 4

http:
  method: PUT
  url: "{{API_URL}}/{{USER_PERMISSION_ENDPOINT}}/sync"
  body:
    type: json
    data: |-
      {
        "username": "admin001",
        "permissions": [
          {
            "featureCode": "CATEGORY",
            "action": "READ",
            "decision": "ALLOW",
            "section": null
          },
          {
            "featureCode": "CATEGORY",
            "action": "DELETE",
            "decision": "DENY",
            "section": "6100"
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
